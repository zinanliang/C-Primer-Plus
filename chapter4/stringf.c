#include<stdio.h>
#define BLURB "Authentic imitation!"
int main(void) 
{
   int n,m;
   char c1,c2;

   //scanf("The first num is %d, the other is %d",&n,&m);
   //printf("Those tow characters are %d %d",n,m);
   scanf("%c,%c",&c1,&c2);
   printf("%c%c",c1,c2);

    return 0;
}